[System.Serializable]
public class DialogueText
{
    //these variables are case sensitive and must match the strings "order" and "text" in the JSON.
    public string character;
    public int order;
    public string text;
}
